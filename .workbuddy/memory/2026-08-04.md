# 2026-08-04

## 项目优化体检（pomelo Flutter 音乐播放器）
对 lib/（154 个 dart 文件，约 33k 行）做了可优化点体检，并用 grep 验证关键结论。未修改任何文件。

## 已执行的高优先级优化（2026-08-04 本轮）
- 依赖瘦身：删 5 零引用(bonsoir/audio_service_mpris/json_repair_flutter/riverpod_annotation/json_annotation)+logging；删 flutter_riverpod 直接依赖（10 文件 import→hooks_riverpod）；dependencies 移除 sqflite/sqlite3/flutter_rust_bridge（保留 override）；清理残留注释。
- 性能：home_page 移动端 ListView(children)→ListView.builder；playable_track_tile 整体 watch→.select 只取 activeTrack 匹配。
- lint：analysis_options 启用 5 规则；async_value_ext.whenData→whenOrDefault（改名+支持自定义 loading/error）；album/playlist 详情页示范改用 whenOrDefault 删重复 loading 分支。
- 验证：fvm flutter pub get 成功；flutter analyze 0 error（4 info 为 drift 既有 deprecated，无关）。
- 未做：删重复 js_engine+example 页（用户未勾选）；其余 60+ 处 loading/error 样板未全面替换。

### 高优先级（已 grep 验证）
- 依赖瘦身：5 个零引用直接依赖 `bonsoir`/`audio_service_mpris`/`json_repair_flutter`/`riverpod_annotation`/`json_annotation` 可删。
- `flutter_riverpod` 与 `hooks_riverpod` 重复（后者已 re-export 前者），10 个文件单独 import `flutter_riverpod`，可统一删除该直接依赖、改为 import `hooks_riverpod`。
- `sqflite`/`sqlite3`/`flutter_rust_bridge` 仅被 drift 间接使用，可从 dependencies 移除（`flutter_rust_bridge` 保留 dependency_overrides 以对齐 metadata_god/rusty_dlna 版本）。
- `logging` 依赖仅服务于死代码（YoutubeExplode 日志转发，项目无此依赖），可删。
- 重复代码：`lib/modules/music_lx/model/{js_engine,polyfill,preload}.dart` 与 `lib/services/js_engine/` 三件套完全重复（约 1500 行）；生产路径用 services 版，modules 版仅示例页用。删除 modules 版 + `lib/ui/example/` + `flutter_js` 依赖。
- 性能：`home_page.dart` 移动端用非 lazy `ListView(children)`；`playable_track_tile.dart:75` 整体 watch `audioPlayerProvider` 导致长列表 O(n) 重建，应改 `.select`；`cover_image` 未做解码降采样（缺 cacheWidth/memCacheWidth）。

### 中优先级
- 死扩展 `AsyncValueExt.whenData` 与 Riverpod 自带方法同名永不生效应改名；25 处 loading / 40 处错误 UI 样板可复用。
- 响应式网格列数计算重复 28 处；搜索四联体列表可泛型化省约 350 行。
- 双 DLNA（`dlna_dart`/`rusty_dlna`）在产，应明确主次或删其一。
- 异常静默吞（184 catch 约半数无 logger）；445 处 `as` 强转未用已配的 json_serializable。
- `analysis_options.yaml` 未启用任何自定义 lint 规则。

### 低优先级/现代化
- 测试几乎为零（`test/fluxy_boot_test.dart` 为别项目遗留，无 expect、有 print）。核心纯 Dart 逻辑（pagination/range_headers/鉴权）应补测。
- `sealed class HomeNav` 用手工 `if` 分派，应改 `switch` 穷尽匹配；可用 `extension type` 做 ID 类型安全。
